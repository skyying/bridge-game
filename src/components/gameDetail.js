// export const getGameInfo = props => {
//   let {linkId, table} = props;
//   let gameList = table[linkId].map(obj => Object.assign({}, obj));
//   console.log("gameList in getGameInfo", gameList);
//   return {
//     game: gameList[gameList.length - 1],
//     gameIndex: gameList.length - 1
//   };
// };

// export const getGame = props => getGameInfo(props).game;
// export const getGameIndex = props => getGameInfo(props).gameIndex;
